import MemeberLayout from "@/components/layouts/MemberLayout";

const TransactionMemberview = (props: any) => {
  const { data } = props;
  return (
    <MemeberLayout>
      <h1>Transaction</h1>
    </MemeberLayout>
  );
};

export default TransactionMemberview;
